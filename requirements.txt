# Database & Vector Store
psycopg[binary,pool]>=3.1.18  # Modern PostgreSQL driver with Async and Connection Pool support
pgvector>=0.2.5             # Official pgvector support for Psycopg 3 (provides register_vector_async)

# Document Parsing & Chunking
PyMuPDF>=1.24.0             # Extremely fast PDF parser (fitz)
langchain-text-splitters>=0.0.1  # For RecursiveCharacterTextSplitter

# LLM & Embeddings (GCP Vertex AI)
google-genai>=0.2.0              # Modern Google GenAI SDK (API Key based)

# Configuration & Utilities
python-dotenv>=1.0.1        # Manage environment variables (.env)
pydantic>=2.6.4             # Data validation
pydantic-settings>=2.2.1    # Strongly typed config management
tenacity>=8.2.3             # Retry logic for GCP API Rate Limits
tqdm>=4.66.2                # Progress bars for CLI scripts
loguru>=0.7.2               # Modern logging framework

# Dev Tools (Linting & Formatting)
ruff>=0.3.4                 # The fastest Python linter and formatter written in Rust

# Testing
pytest>=8.1.1                 # Standard testing framework
pytest-asyncio>=0.23.6        # Asyncio support for pytest
