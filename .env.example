APP_ENVIRONMENT=dev

# Database Settings (Local direct access or via GCP VPC)
DB_HOST=10.195.208.3
DB_PORT=5432
DB_NAME=rag_evaluation_db
DB_USER=nvd11
DB_PASSWORD=hsbc1234

# GCP Settings
GCP_PROJECT=jason-hsbc
GCP_REGION=europe-west2
EMBEDDING_MODEL=text-embedding-004
LLM_JUDGE_MODEL=gemini-1.5-pro-preview-0409

# Global Proxy Settings (for LLM/Vertex AI access from restricted regions)
# Set ENABLE_PROXY=true in your local .env to route traffic through the proxy
ENABLE_PROXY=false
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
