APP_ENVIRONMENT=dev

# Database Settings (Local direct access or via GCP VPC)
DB_HOST=db.jpgcp.cloud
DB_PORT=5432
DB_NAME=rag_evaluation_db
DB_USER=nvd11
DB_PASSWORD=hsbc1234

# Google AI Settings (Using API Key instead of GCP Service Account for secure code sharing)
GEMINI_API_KEY=your_gemini_api_key_here
EMBEDDING_MODEL=models/gemini-embedding-001
EMBEDDING_DIMENSION=768
EMBEDDING_BATCH_SIZE=100
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
LLM_JUDGE_MODEL=gemini-2.5-pro

# Global Proxy Settings (for LLM access from restricted regions)
# Set ENABLE_PROXY=true in your local .env to route traffic through the proxy
ENABLE_PROXY=false
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
